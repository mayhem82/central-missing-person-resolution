name: Source change watch
on:
  workflow_dispatch:
  push:
    paths:
      - 'scripts/snapshot_sources.js'
      - 'scripts/ingest_setu.js'
      - 'data/source-manifest.json'
      - '.github/workflows/source-watch.yml'
  schedule:
    - cron: '17 * * * *'
permissions:
  contents: write
jobs:
  snapshot:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Snapshot authoritative sources
        run: node scripts/snapshot_sources.js
        continue-on-error: true
      - name: Build Setu incident dataset and source diff
        run: node scripts/ingest_setu.js
      - name: Commit changed snapshots and normalized data
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add data/source-snapshots data/source-snapshot-state.json data/setu-latest.json data/setu-diff.json
          if git diff --cached --quiet; then
            echo 'No source or normalized-data changes.'
          else
            git commit -m 'source-watch: snapshot and normalize current incident data'
            git push
          fi
